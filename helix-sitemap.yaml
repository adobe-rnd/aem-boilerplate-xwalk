sitemaps:
  example:
    default: en
    lastmod: YYYY-MM-DD  # Dynamic date to be updated for each request or generation
    languages:
      en:
        source: /query-index.json
        destination: /sitemap-master.xml
        hreflang: en
      hi:
        source: /hi/query-index.json
        destination: /hi/sitemap-master.xml
        alternate: /hi/{path}
        hreflang: hi
      gu:
        source: /gu/query-index.json
        destination: /gu/sitemap-master.xml
        alternate: /gu/{path}
        hreflang: gu
      mr:
        source: /mr/query-index.json
        destination: /mr/sitemap-master.xml
        alternate: /mr/{path}
        hreflang: mr
      ta:
        source: /ta/query-index.json
        destination: /ta/sitemap-master.xml
        alternate: /ta/{path}
        hreflang: ta
      te:
        source: /te/query-index.json
        destination: /te/sitemap-master.xml
        alternate: /te/{path}
        hreflang: te
      kn:
        source: /kn/query-index.json
        destination: /kn/sitemap-master.xml
        alternate: /kn/{path}
        hreflang: kn
      ml:
        source: /ml/query-index.json
        destination: /ml/sitemap-master.xml
        alternate: /ml/{path}
        hreflang: ml
version: 1
indices:
  banners:
    target: /banners.json
    include:
      - '/banners
    properties:
      title:
        select: head > meta[property="og:title"]
        value: |
          attribute(el, 'content')
      url:
        select: head > meta[property="og:url"]
        value: |
          attribute(el, 'content')
